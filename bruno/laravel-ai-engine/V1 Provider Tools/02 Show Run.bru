meta {
  name: Show Run
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/ai/provider-tools/runs/{{providerToolRunId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
