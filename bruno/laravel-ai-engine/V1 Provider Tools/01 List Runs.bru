meta {
  name: List Runs
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/ai/provider-tools/runs?status={{providerToolStatus}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
