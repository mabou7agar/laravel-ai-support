meta {
  name: Upload
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/ai-demo/chat/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:multipart-form {
  file: @file({{filePath}})
  session_id: {{chatSessionId}}
}
