meta {
  name: Context Summary
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/ai-demo/chat/context-summary/{{chatSessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
