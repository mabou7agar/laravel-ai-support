meta {
  name: History
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ai-demo/chat/history/{{chatSessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
