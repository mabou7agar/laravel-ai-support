meta {
  name: Send
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/ai-demo/chat/send
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "message": "List invoices",
    "session_id": "{{chatSessionId}}"
  }
}
