meta {
  name: Clear
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/ai-demo/chat/clear
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "session_id": "{{chatSessionId}}"
  }
}
