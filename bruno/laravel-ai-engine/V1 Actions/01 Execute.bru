meta {
  name: Execute
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/actions/execute
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "action_type": "{{actionType}}",
    "data": {
      "model_id": 1,
      "model_class": "App\\Models\\User"
    },
    "session_id": "{{chatSessionId}}"
  }
}
