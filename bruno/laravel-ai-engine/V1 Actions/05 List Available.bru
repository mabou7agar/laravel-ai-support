meta {
  name: List Available
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/actions/available?context=email&include_remote=false
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
