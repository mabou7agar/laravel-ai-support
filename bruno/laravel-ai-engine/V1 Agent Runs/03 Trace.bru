meta {
  name: Trace
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/ai/agent-runs/{{agentRunId}}/trace
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
