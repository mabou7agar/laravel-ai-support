meta {
  name: Stream Run
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/ai/agent-runs/{{agentRunId}}/stream?timeout=30&poll=500
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: text/event-stream
}
