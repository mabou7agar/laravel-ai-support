meta {
  name: Generate Token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/token
  body: json
}

headers {
  Accept: application/json
}

body:json {
  {
    "user_id": "{{userId}}"
  }
}
