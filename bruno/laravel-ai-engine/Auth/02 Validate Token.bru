meta {
  name: Validate Token
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/auth/validate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
