meta {
  name: Clear History
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/rag/chat/clear
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "session_id": "{{ragSessionId}}"
  }
}
