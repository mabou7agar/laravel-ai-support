meta {
  name: Status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ai-demo/workflow/status/{{workflowJobId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
