meta {
  name: Status
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/data-collector/status/{{dataCollectorSessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}
