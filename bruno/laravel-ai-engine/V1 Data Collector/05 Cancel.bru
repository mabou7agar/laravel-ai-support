meta {
  name: Cancel
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/data-collector/cancel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

body:json {
  {
    "session_id": "{{dataCollectorSessionId}}"
  }
}
