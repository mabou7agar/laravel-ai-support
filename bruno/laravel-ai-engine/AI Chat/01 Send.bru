meta {
  name: Send
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/ai-chat/send
  body: json
}

headers {
  Accept: application/json
  X-CSRF-TOKEN: {{csrfToken}}
  Cookie: {{webCookie}}
}

body:json {
  {
    "message": "List invoices",
    "session_id": "{{chatSessionId}}"
  }
}
