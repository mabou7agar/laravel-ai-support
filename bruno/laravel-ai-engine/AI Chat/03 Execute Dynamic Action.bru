meta {
  name: Execute Dynamic Action
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/ai-chat/execute-action
  body: json
}

headers {
  Accept: application/json
  X-CSRF-TOKEN: {{csrfToken}}
  Cookie: {{webCookie}}
}

body:json {
  {
    "action_type": "{{actionType}}",
    "action_data": {
      "model_id": 1,
      "model_class": "App\\Models\\User"
    },
    "session_id": "{{chatSessionId}}"
  }
}
